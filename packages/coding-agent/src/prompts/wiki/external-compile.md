# External Wiki Compile

One bounded run over the vault at `{{vault}}`. `AGENTS.md` in that directory is
the spec: eligibility, note shape, scope and secret rules, the ordered steps,
the pre-finish verification, and the summary shape. Follow it exactly.
